{% extends 'layout.twig' %}

{% block title %}{{parent()}} - Details Question{% endblock title %}

{% block body %}
    <div class="container">
    	<h1 class="mb15">{{itemName}}</h1>
    </div>
    <div class="container" ng-controller="detailsQuestionController">
            {% for content in question %}
                <p>{{content.name}}</p>
        {% endfor %}
    </div>
{% endblock %}